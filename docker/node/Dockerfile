ARG NODE_VERSION="latest"

FROM node:${NODE_VERSION}

ARG DIRECTORY_PATH=/usr/src/app
ENV DIRECTORY_PATH=$DIRECTORY_PATH

WORKDIR $DIRECTORY_PATH

COPY . $DIRECTORY_PATH

ENTRYPOINT ./docker/node/script.sh $DIRECTORY_PATH
